# LogLens AI — Python Dependencies
# Install with: pip3 install -r requirements.txt

# ── Core server ───────────────────────────────────────────────────────────────
fastapi>=0.104.0           # Web framework for the inference API
uvicorn[standard]>=0.24.0  # ASGI server (runs FastAPI)
requests>=2.31.0           # HTTP client
pydantic>=2.0.0            # Data validation (used by FastAPI)

# ── Attention module + Training ───────────────────────────────────────────────
torch>=2.1.0               # Core deep learning
transformers>=4.40.0       # Tokenizer + HuggingFace utilities
safetensors>=0.4.0         # Safe model weight format (vLLM native)
huggingface_hub>=0.20.0    # Upload/download from HuggingFace

# ── RAG Pipeline ─────────────────────────────────────────────────────────────
chromadb>=0.4.0            # Local vector database (ingest_logs.py, rag_query.py)
sentence-transformers>=2.2.0  # Embedding model (all-MiniLM-L6-v2)
tqdm>=4.65.0               # Progress bars during ingestion
openai>=1.0.0              # Client to call vLLM RunPod endpoint

# ── Optional: real LLM via local Ollama ──────────────────────────────────────
# No pip package needed — install Ollama separately:
#   Mac:   brew install ollama && ollama pull llama3.2:1b
#   Linux: curl -fsSL https://ollama.ai/install.sh | sh

# ── Optional: FlashAttention (faster attention kernel) ───────────────────────
# pip install flash-attn --no-build-isolation   (not on all platforms)

# ── Optional: AWS Trainium (needed for Section 4 of README) ──────────────────
# torch-neuronx    # Only runs on AWS trn1 instances
# neuronx-cc       # Trainium compiler
